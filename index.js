import {AppRegistry} from 'react-native';
import App from './App';
// eslint-disable-line @typescript-eslint/no-unused-vars
import i18n from '~/lib/i18n';

const appName = 'RNTemplate';
AppRegistry.registerComponent(appName, () => App);
